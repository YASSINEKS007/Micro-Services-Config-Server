spring:
  application:
    name: customer-service

  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:customers-db
  cloud:
    discovery:
      enabled: true
server:
  port: 8081
